model Level {
  id              String        @id @default(uuid()) @db.Uuid
  title           String
  createdAt       DateTime      @default(now())
  updatedAt       DateTime      @updatedAt
  islandPositions IslandLevel[]
  slides          LevelSlide[]
}
